{
  "lexicon": 1,
  "id": "org.radlib.private.banMember",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Ban a community member from the community's permissioned space. This is community-local moderation.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["space", "target"],
          "properties": {
            "space": { "type": "string" },
            "target": { "type": "string", "format": "did" },
            "reason": { "type": "string", "maxLength": 1000 },
            "expiresAt": { "type": "string", "format": "datetime" }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["space", "target", "state"],
          "properties": {
            "space": { "type": "string" },
            "target": { "type": "string", "format": "did" },
            "state": { "type": "string", "knownValues": ["requested", "approved", "denied", "removed", "banned"] }
          }
        }
      }
    }
  }
}
